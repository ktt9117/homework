package com.nhnent.exam.netlibrary;

import org.junit.Test;

/**
 * Created by gradler on 18/08/2017.
 */
public class ImageLoaderTest {

    @Test
    public void thumbnailLoadTest() {
        /*
        final CountDownLatch signal = new CountDownLatch(1);
        ImageLoader loader = new ImageLoader();

        loader.loadThumbnail(null, "https://upload.wikimedia.org/wikipedia/en/thumb/c/c5/Gmail_screenshot.png/320px-Gmail_screenshot.png",
                new ImageLoader.OnResultListener() {
                    @Override
                    public void onResult(Bitmap bitmap) {
                        assertEquals(true, (bitmap != null));
                        signal.countDown();
                    }
                });
        try {
            signal.await();
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        */
    }
}